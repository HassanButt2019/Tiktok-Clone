


import 'package:flutter/material.dart';

class KWebMessageScreen extends StatelessWidget {
  const KWebMessageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Message Screen"),
      ),
    );
  }
}
